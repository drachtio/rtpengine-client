{
  "name": "rtpengine-client",
  "version": "0.0.2",
  "description": "node client for rtpengine daemon",
  "main": "index.js",
  "scripts": {
    "jslint": "eslint lib index.js",
    "test": "NODE_ENV=test,DEBUG=* node test/* | ./node_modules/.bin/tap-spec",
    "coverage": "NODE_ENV=test ./node_modules/.bin/istanbul cover test/* --report html",
    "coveralls": "NODE_ENV=test ./node_modules/.bin/istanbul cover test/* && node_modules/.bin/istanbul-coveralls"
  },
  "author": "Dave Horton",
  "license": "MIT",
  "devDependencies": {
    "coveralls": "^2.13.1",
    "istanbul": "^0.4.5",
    "sinon": "^3.2.0",
    "tap": "^10.7.0",
    "tap-dot": "^1.0.5",
    "tap-spec": "^4.1.1",
    "tape": "^4.7.0"
  },
  "dependencies": {
    "bencode": "^1.0.0",
    "dgram": "^1.0.1"
  }
}
